This module returns a list of issues. The function issue() returns a JSON list of
all the issues stored in memory.

There is also another function that creates an issue. The function create_issue() 
creates a new issue using the Issue class, and returns that issue.

